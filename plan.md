The current app was auto generated by clod code. While it contains a lot of the functionality we want for the project there is also a lot of bloats and features that we might not necessarily end up wanting to keep.

# package.json